// Code generated by go-swagger; DO NOT EDIT.

package models

import (
	"context"

	"github.com/go-openapi/strfmt"
	"github.com/go-openapi/swag"
)

// ConfigPreviewServerConfig config preview server config
//
// swagger:model config.PreviewServerConfig
type ConfigPreviewServerConfig struct {

	// CustomDomain is the base domain the preview server expects on the
	// leading label of an inbound Host, e.g. "preview.signadot.com". Empty
	// means none is configured, and preview URLs are reported on Signadot's
	// own domain.
	CustomDomain string `json:"customDomain,omitempty"`

	// Enabled is whether previews are served for this cluster: the control
	// plane forwards preview requests to the preview server when true and
	// refuses them when false.
	//
	// A pointer so an absent stanza is distinguishable from an explicit
	// false. Absent means an operator or a chart predating this field, which
	// keeps the behavior it has always had -- see PreviewServerEnabled.
	//
	// An explicit value, either way, is only renderable by a chart that ships
	// previewserver, so a false describes a cluster that has a preview server
	// to turn off. The control plane relies on that but does not assume it: it
	// consults this stanza only for operators new enough to have written it,
	// which is also what makes a hand-edited ConfigMap on an older cluster
	// inert. The implication runs one way only -- v1.4.0 itself renders
	// nothing here -- so presence cannot be derived from the agent version and
	// absence has to be read as its own state.
	Enabled bool `json:"enabled,omitempty"`
}

// Validate validates this config preview server config
func (m *ConfigPreviewServerConfig) Validate(formats strfmt.Registry) error {
	return nil
}

// ContextValidate validates this config preview server config based on context it is used
func (m *ConfigPreviewServerConfig) ContextValidate(ctx context.Context, formats strfmt.Registry) error {
	return nil
}

// MarshalBinary interface implementation
func (m *ConfigPreviewServerConfig) MarshalBinary() ([]byte, error) {
	if m == nil {
		return nil, nil
	}
	return swag.WriteJSON(m)
}

// UnmarshalBinary interface implementation
func (m *ConfigPreviewServerConfig) UnmarshalBinary(b []byte) error {
	var res ConfigPreviewServerConfig
	if err := swag.ReadJSON(b, &res); err != nil {
		return err
	}
	*m = res
	return nil
}
